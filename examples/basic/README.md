This example demonstrates how the `toBlob` and `toBase64` helpers work in a browser environment.

Open `demo.html` in a browser (no build required).

- `demo.html` shows an input area where you can type text, convert it to a `Blob`, then convert back to a base64 data URL and show the result.

The demo contains inline, minimal implementations equivalent to the library so it runs without bundling.